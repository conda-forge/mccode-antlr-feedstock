{% set version = "0.25.0" %}

package:
  name: mccode-antlr
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/m/mccode_antlr/mccode_antlr-{{ version }}.tar.gz
  sha256: ab795fc89078ad4d64838ab7b03ef94e4e0f2f31b4b377f886f5e93e7804455f

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - mcstas-antlr = mccode_antlr.cli.commands:mcstas
    - mcxtrace-antlr = mccode_antlr.cli.commands:mcxtrace
    - mcrun-antlr = mccode_antlr.run.runner:mcstas_cmd
    - mxrun-antlr = mccode_antlr.run.runner:mcxtrace_cmd
    - mccode-antlr = mccode_antlr.cli.management:mccode_management

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools >=64.0
    - setuptools_scm >=6.2
    - antlr4-python3-runtime >=4.13.2
    - antlr4-tools
  run:
    - python >={{ python_min }}
    - numpy >=2.0
    - antlr4-python3-runtime >=4.13.2
    - pooch >=1.7.0
    - confuse >=2.2.1
    - loguru >=0.7.2
    - gitpython >=3.1.43
    - msgspec >=0.19.0
    - networkx >=3.0
    - sympy >=1.12
    - rich >= 13.0.0

test:
  imports:
    - mccode_antlr
  commands:
    - pip check
    - pytest tests
    - mcstas-antlr --help
    - mcxtrace-antlr --help
    - mcrun-antlr --help
    - mxrun-antlr --help
    - mccode-antlr --help
  source_files:
    - tests
  requires:
    - python {{ python_min }}
    - git
    - pytest
    - pip
    - c-compiler
    - h5py

about:
  home: https://github.com/mccode-dev/mccode-antlr
  summary: McCode grammar implemented with ANTLR4
  description: |
    The mccode-antlr package provides a command-line interface to the McCode
    language parsers and translators.
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  doc_url: https://github.com/mccode-dev/mccode-antlr
  dev_url: https://github.com/mccode-dev/mccode-antlr

extra:
  recipe-maintainers:
    - g5t
    - willend
    - tkittel
